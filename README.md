
  # Apply specified color combo

  This is a code bundle for Apply specified color combo. The original project is available at https://www.figma.com/design/bJQS37ixova2arQRHh18Fh/Apply-specified-color-combo.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  